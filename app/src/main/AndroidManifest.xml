<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="no.madrobot.vitaminator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver android:name=".AlertReciever"
            android:enabled="true"
            android:exported="true"/>
        <activity android:name=".Check_daily_status" />
        <activity android:name=".Take_a_pill" />
        <activity android:name=".List_of_pills" />
        <activity android:name=".Add_a_pill" />
        <activity android:name=".Remove_a_pill"></activity>


    </application>

</manifest>